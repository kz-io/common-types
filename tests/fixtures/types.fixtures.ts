/**
 * This file contains test type implementations for interfaces and type aliases in the package.
 *
 * @copyright 2024 integereleven. All rights reserved. MIT license.
 */

import type {
  Codebase,
  DecoratorTarget,
  IHelpful,
  IndeterminateObject,
  SoftwareOperation,
  SystemArchitecture,
  SystemOS,
} from '../../mod.ts';

const _typeFixture0: IndeterminateObject = {
  name: 'Dakota Cortez',
  age: 15,
  username: 'dc655',
};

const _typeFixture1: DecoratorTarget = 'class';

const _typeFixture2: DecoratorTarget = 'method';

const _typeFixture3: DecoratorTarget = 'property';

const _typeFixture4: DecoratorTarget = 'parameter';

const _typeFixture5: DecoratorTarget = 'accessor';

const _typeFixture6: Codebase = 'anything';

const _typeFixture7: Codebase = 'everything';

const _typeFixture8: SoftwareOperation = 'anything';

const _typeFixture9: SoftwareOperation = 'everything';

const _typeFixture10: IHelpful = {
  helpUrl: 'https://example.com',
};

const _typeFixture11: SystemOS = 'aix';

const _typeFixture12: SystemOS = 'android';

const _typeFixture13: SystemOS = 'darwin';

const _typeFixture14: SystemOS = 'freebsd';

const _typeFixture15: SystemOS = 'illumos';

const _typeFixture16: SystemOS = 'linux';

const _typeFixture17: SystemOS = 'netbsd';

const _typeFixture18: SystemOS = 'solaris';

const _typeFixture19: SystemOS = 'windows';

const _typeFixture20: SystemArchitecture = 'aarch64';

const _typeFixture21: SystemArchitecture = 'x86_64';
